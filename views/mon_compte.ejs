<%- include('header') %>
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Compte</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 80%;
            margin: 0 auto;
        }
        .section-title {
            background-color: #ff6600;
            color: white;
            padding: 10px;
            font-size: 1.2em;
            margin-bottom: 10px;
        }
        .profile-section, .info-section, .reviews-section {
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 50px;
        }
        .profile-picture {
            float: left;
            margin-right: 20px;
        }
        .profile-picture img {
            border-radius: 50%;
            width: 100px;
            height: 100px;
        }
        .profile-info {
            overflow: hidden;
            height: 60px;
        }
        .profile-info p {
            margin: 0;
        }
        .profile-info .username {
            color: rgb(0, 132, 255);
            background-color: #e9e9e9;
            border-radius: 5px;
            width:175px;
            font-size: 1.2em;
        }
        .info-section p {
            margin: 5px 0;
        }
        .reviews-section{
            background-color: #d6d6d6;
        }
        .reviews-section .review {
            display: inline-block;
            background-color: #505050;
            padding: 5px;
            border-radius: 5px;
            width: 45%;
            margin-right: 5%;
            margin-bottom: 10px;
        }
        .reviews-section .review:last-child {
            margin-right: 0;
        }
        .reviews-section .review h3 a {
            color: #ff6600;
        }
        .delete_account {
            margin-top: 20px;
        }
        .delete_account a {
            color: red;
            text-decoration: none;
        }
        .delete_account a:hover {
            text-decoration: underline;
        }
        
    </style>
</head>
<body>

<div class="container">
    <!-- Section Mon profil -->
    <div class="section-title">Mon profil</div>
    <div class="profile-section">
        <div class="profile-picture">
            <img src="/images_profil/profil_1.png" alt="Profile Icon">
        </div>
        <div class="profile-info">
            <p class="username"><%= user.username %></p>
            <!--<p>Inscrit depuis le <span id="date_inscription_user">date_inscription_user</span></p>-->
            <p><span id="nb_avis_user"><%= user.nb_avis %></span> avis</p>
            <p><span >Email : </span><%= user.mail_user %></span></p>
        </div>
        <div class="delete_account">
            <a href="/delete_account">Supprimer mon compte</a>
        </div>
    </div>

<!-- Section Mes derniers avis -->
<div class="section-title">Mes derniers avis</div>
<div class="reviews-section">
    <% if (reviews && reviews.length > 0) { %>
        <% reviews.forEach(function(review) { %>
            <div class="review">
                <h3><a href="/jeu/<%= review.ID_jeu %>#reviews-list"><%= review.titre %></a></h3>
                <p style="color: white;"><%= review.avis_user %></p> <!-- Utiliser `avis_user` ici -->
                <p style="color: white;">
                    Note :
                    <% for (let i = 1; i <= 5; i++) { %>
                        <% if (i <= review.note_user) { %>
                            <span style="color: gold;">&#9733;</span> <!-- étoile pleine -->
                        <% } else { %>
                            <span style="color: lightgray;">&#9733;</span> <!-- étoile vide -->
                        <% } %>
                    <% } %>
                </p>
                
            </div>
        <% }); %>
    <% } else { %>
        <p>Vous n'avez pas encore laissé d'avis.</p>
    <% } %>
</div>

</div>


</body>
</html>
