<!DOCTYPE html>
<html lang="fr">
<head>
    <title>Page d'Inscription</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        h1 {
            text-align: center;
            margin-top: 50px;
        }
        form {
            width: 300px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        label {
            font-weight: bold;
        }
        input {
            width: 100%;
            padding: 10px;
            margin: 5px 0 20px -10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        input[type="submit"], button {
            background-color: orange; /* Couleur de fond orange */
            color: #fff;
            cursor: pointer;
            margin: 0 auto;
            display: block;
            border-radius: 5px;
            border: none; /* Pas de bordure */
            width: 100%;
            padding: 10px;
        }
        input[type="submit"]:hover, button:hover {
            background-color: darkorange; /* Couleur au survol orange foncé */
        }
        p {
            color: red;
            text-align: center;
        }
        .cookie {
            text-align: center;
            color: #667;
            display: none; /* Hide the cookie consent initially */
            position: fixed; /* Fixed position to show as a popup */
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            background-color: #fff;
            padding: 10px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            z-index: 999; /* Ensure it's on top of other elements */
        }
        .login {
            text-align: center;
            color: #000;
        }
        footer {
            text-align: center;
            margin-top: 20px;
            padding: 10px;
            background-color: #fff;
            border-top: 1px solid #ddd;
        }
    </style>
    <script>
        // JavaScript function to show the cookie consent popup
        function showCookiePopup() {
            document.getElementById("cookiePopup").style.display = "block";
        }

        // JavaScript function to hide the cookie consent popup
        function hideCookiePopup() {
            document.getElementById("cookiePopup").style.display = "none";
        }
    </script>
    <link rel="icon" type="image/x-icon" href="appESGITutor.ico">
</head>
<body>
    <h1>Page d'Inscription</h1>
    <form method="POST">
        <label for="username">Nom d'utilisateur:</label>
        <input type="text" id="username" name="username" required>
        
        <label for="email">Adresse mail:</label>
        <input type="email" id="email" name="email" required>
        
        <label for="password">Mot de passe:</label>
        <input type="password" id="password" name="password" required>

        <label for="password_confirm">Tapez à nouveau votre mot de passe:</label>
        <input type="password" id="password_confirm" name="password_confirm" required>

        <button type="submit">Je m'inscris</button>
    </form>

    <p class="login">Vous avez déjà un compte ? <a href="#">Connectez-vous</a></p>

    <div id="cookiePopup" class="cookie">
        En utilisant ce site web, vous acceptez notre utilisation des cookies.
        <a href="#" onclick="hideCookiePopup()">Compris</a>
        <a href="#">En savoir plus</a>
    </div>

    <script>
        // Show the cookie consent popup when the page loads
        window.onload = function() {
            showCookiePopup();
        };
    </script>

    <%- include('footer') %>

</body>
</html>
